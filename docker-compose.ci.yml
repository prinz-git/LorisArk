services:
  api:
    environment:
      APP_ENV: "test"
      SECRET_KEY: "ci-secret"
      DATABASE_URL: "sqlite:////tmp/test_lorisark.db"

  frontend:
    environment:
      NEXT_PUBLIC_API_BASE_URL: "http://localhost:8000"
